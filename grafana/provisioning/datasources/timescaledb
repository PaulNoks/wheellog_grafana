apiVersion: 1

datasources:
  - name: TimescaleDB
    type: postgres
    access: proxy
    url: host.docker.internal:5432
    database: wheellog
    user: pavel
    secureJsonData:
      password: 'password'  # Замените на свой пароль
    jsonData:
      sslmode: 'disable'
      postgresVersion: 1400
      timescaledb: true
    editable: true
